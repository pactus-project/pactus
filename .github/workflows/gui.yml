name: Build and Test GUI
permissions:
  contents: read

on:
  push:
    branches: ["main"]
  pull_request:
    branches: ["main"]

jobs:
  build-gui:
    runs-on: ${{ matrix.name }}
    defaults:
      run:
        shell: ${{ matrix.shell }}
    strategy:
      matrix:
        # Default values for `defaults.run.shell`
        # https://docs.github.com/en/actions/writing-workflows/workflow-syntax-for-github-actions#defaultsrunshell
        include:
          - name: ubuntu-latest
            shell: "bash -e {0}"
          - name: macos-latest
            shell: "bash -e {0}"
          - name: windows-latest
            shell: "msys2 {0}"

    steps:
      - uses: actions/checkout@v6

      - name: Setup Go for macOS and Linux
        if: runner.os != 'Windows'
        uses: actions/setup-go@v6
        with:
          go-version: "1.26.2"

      - name: Install Dependencies (Linux)
        if: runner.os == 'Linux'
        run: |
          sudo apt update
          sudo apt install -y libgtk-4-dev libcairo2-dev libglib2.0-dev gobject-introspection libgtksourceview-5-dev

      - name: Install Dependencies (macOS)
        if: runner.os == 'macOS'
        run: brew install gtk+4 gobject-introspection gtksourceview5

      - name: Setup MSYS2 and Dependencies (Windows)
        if: runner.os == 'Windows'
        uses: msys2/setup-msys2@v2
        with:
          msystem: MINGW64
          install: >-
            git
            make
            glib2-devel
            mingw-w64-x86_64-go
            mingw-w64-x86_64-gtk4
            mingw-w64-x86_64-glib2
            mingw-w64-x86_64-gcc
            mingw-w64-x86_64-pkg-config
            mingw-w64-x86_64-gtksourceview5
            mingw-w64-x86_64-gobject-introspection

      #######################################################
      ## Caching Go modules and GTK dependencies

      - name: Get Go environment
        id: go-env
        run: |
          echo "cache=$(go env GOCACHE)" >> $GITHUB_ENV
          echo "lintcache=$HOME/.cache/golangci-lint" >> $GITHUB_ENV

      # Example for caching Go dependencies:
      # https://github.com/actions/cache/blob/main/examples.md#go---modules
      - uses: actions/cache@v5
        id: cache
        with:
          path: |
            ${{ env.cache }}
            ${{ env.lintcache }}
          key: ${{ matrix.name }}-go-${{ hashFiles('**/go.sum') }}
          restore-keys: |
            ${{ matrix.name }}-go-
      #######################################################
      - name: Install GTK
        if: steps.cache.outputs.cache-hit != 'true'
        run: |
          go install "github.com/diamondburned/gotk4/pkg/gtk/v4"
          go install "github.com/diamondburned/gotk4/pkg/gdk/v4"
          go install "github.com/diamondburned/gotk4/pkg/glib/v2"

      - name: Build the GUI binary
        run: make build_gui

      - name: Test the GUI binary
        run: go test -tags gtk ./cmd/gtk/...

      - name: Lint check (Linux only)
        if: runner.os == 'Linux'
        uses: golangci/golangci-lint-action@v9
        with:
          version: v2.11.4
          args: --build-tags gtk
