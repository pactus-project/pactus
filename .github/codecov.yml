comment:
  layout: header, changes, diff, sunburst
coverage:
  status:
    patch:
      default:
        threshold: "10%"
        only_pulls: true

    project:
      default:
        target: auto
        threshold: "10%"

ignore:
  - "**/*_mock.go"
  - "**/*_fake.go"
  - "www/grpc/gen"
